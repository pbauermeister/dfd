# Issue #127: G is merged away by the first merge, so it cannot receive
# the second one. A chain is written forward.
#include tests/non-regression/093-filter-merge-master.part

merge G D : H
merge B C : G
