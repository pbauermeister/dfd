#include tests/non-regression/111-filter-layout-matrix.part

# layout neighborhood, both sides, two levels: keeps the whole chain
# only where both arrows are drawn the same way (1 and 4), stops at a
# turn (2 and 3), ignores the constraint (5)
![]2 A1 A2 A3 A4 A5
