# Issue #127: the merged items are in two frames, so the replacer
# inherits none; each frame keeps its other member.
#include tests/non-regression/093-filter-merge-master.part

frame A B = F1
frame C D = F2
merge B C : G
