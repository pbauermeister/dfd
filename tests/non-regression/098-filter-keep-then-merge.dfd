# Issue #127: keep first (A and two levels down: B, C), then merge: the
# replacer takes the place of the kept members. Expected: A -> G.
#include tests/non-regression/093-filter-merge-master.dfd

!>2 A
merge B C : G
