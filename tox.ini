[tox]
envlist = py{311,312,313}
isolated_build = true
minversion = 3.3.0

[testenv]
deps =
    pytest
    typing_extensions
    mypy
commands =
    make test VENV={envdir}
    make lint VENV={envdir}
allowlist_externals =
    make
